// components 
import components from './comps.components';
// components

// directives
import directives from './comps.directives';
// directives

// const { CApp, CContainer, CRow, CCol, CToolbar, CNavDrawer, COverlay, CButton, CSpinner, CIcon, CFormRadio, CListGroup, CListItem } = components;

export default {
	components,
	directives
}